apiVersion: v1
kind: Service
metadata:
  name: {{ include "kotlin-time-service.fullname" . }}
  labels:
    {{- include "kotlin-time-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kotlin-time-service.selectorLabels" . | nindent 4 }}
