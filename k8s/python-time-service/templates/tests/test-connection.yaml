apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-time-service.fullname" . }}-test-connection"
  labels:
    {{- include "python-time-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-time-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
